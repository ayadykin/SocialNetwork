spring:
  application:
    name: redis-service
  cloud:
    config: 
     uri: http://localhost:8080/SNConfig
     fail-fast: true
      #password: ${CONFIG_SERVICE_PASSWORD}
      #username: user
spring.jmx.default-domain: com.social.network.redis
spring:
  application:
    name: monitor-service
  cloud:
    config: 
     uri: http://localhost:8080/SNConfig
     fail-fast: true
      #password: ${CONFIG_SERVICE_PASSWORD}
      #username: user
turbine:
  stream:
    port: 8989
spring.jmx.default-domain: com.social.network.monitor